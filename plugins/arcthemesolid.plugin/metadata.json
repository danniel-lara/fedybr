{
	"label": "Arc Theme",
	"description": "Uma versão não transparente do tema Arc",
	"license": "GPLv3",
	"category": "Temas",
	"scripts": {
		"exec": {
			"label": "Instalar",
			"command": "run-as-root -s install.sh"
		},
		"undo": {
			"label": "Remover",
			"command": "run-as-root -s uninstall.sh"
		},
		"status": { "command": "rpm --quiet --query arc-theme-solid" }
	}
}
