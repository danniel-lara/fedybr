{
	"icon": "nethogs",
	"label": "Nethogs",
	"description": "NetHogs é uma pequena ferramenta net top. (Uso via Terminal) ",
	"license": "GPL",
	"category": "Info/Monitor",
	"scripts": {
		"exec": {
			"label": "Instalar",
			"command": "run-as-root -s install.sh"
		},
		"undo": {
			"label": "Remover",
			"command": "run-as-root -s uninstall.sh"
		},
		"status": { "command": "rpm --quiet --query nethogs" }
	}
}
