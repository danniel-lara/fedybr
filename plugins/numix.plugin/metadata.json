{
	"icon": "numix",
	"label": "Numix themes",
	"description": "GTK e temas de ícones do Projeto Numix",
	"license": "GPLv3",
	"category": "Temas",
	"scripts": {
		"exec": {
			"label": "Instalar",
			"command": "run-as-root -s install.sh"
		},
		"undo": {
			"label": "Remover",
			"command": "run-as-root -s uninstall.sh"
		},
		"status": { "command": "rpm --quiet --query numix-icon-theme numix-icon-theme-circle numix-gtk-theme" }
	}
}
