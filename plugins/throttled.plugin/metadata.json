{
	"icon": "cpu",
	"label": "throttled",
	"description": "Solução alternativa para problemas de limitação da Intel no Linux com notebooks Lenovo.",
	"license": "MIT",
	"category": "Tweaks",
	"scripts": {
		"exec": {
			"label": "Instalar",
			"command": "run-as-root -s install.sh"
		},
		"undo": {
			"label": "Remover",
			"command": "run-as-root -s uninstall.sh"
		},
		"status": { "command": "rpm --quiet --query throttled" }
	}
}
