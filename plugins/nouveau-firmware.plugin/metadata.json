{
	"icon": "video-display",
	"label": "nouveau video firmware",
	"description": "firmwares extraídos do driver binário da NVIDIA para o nouveau ",
	"license": "Proprietario",
	"category": "Drivers",
	"scripts": {
		"exec": {
			"label": "Instalar",
			"command": "run-as-root -s install.sh"
		},
		"undo": {
			"label": "Remover",
			"command": "run-as-root -s remove.sh "
		},
		"status": { "command": "rpm --quiet --query nouveau-firmware" }
	}
}
