{
	"icon": "mongodb",
	"label": "MongoDB",
	"description": "MongoDB é um programa de banco de dados orientado a documentos de plataforma cruzada.",
	"license": "SSPL v1.0",
	"category": "Ferramentas de Desenvolvimento",
	"scripts": {
		"exec": {
			"label": "Instalar",
			"command": "run-as-root -s install.sh"
		},
		"undo": {
			"label": "Remover",
			"command": "run-as-root -s uninstall.sh"
		},
		"status": { "command": "rpm --quiet --query mongodb-org" }
	}
}
