{
	"icon": "video-display",
	"label": "Intel legacy VAAPI driver",
	"description": "Instale o driver herdado da Intel para VAAPI",
	"license": "MIT and EPL",
	"category": "Drivers",
	"scripts": {
		"exec": {
			"label": "Instalar",
			"command": "run-as-root -s install.sh"
		},
		"undo": {
			"label": "Remover",
			"command": "run-as-root -s remove.sh "
		},
		"status": { "command": "rpm --quiet --query libva-intel-driver" }
	}
}
